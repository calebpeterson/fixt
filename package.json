{
  "name": "fixt",
  "version": "1.0.0",
  "description": "Fix the previous command based on a set of rules",
  "main": "index.js",
  "author": "caleb.peterson@zapier.com",
  "license": "MIT",
  "scripts": {
    "main": "echo 'Please run ./fixt directly'",
    "dev:install": "yarn link --force"
  },
  "bin": {
    "fixt": "fixt"
  }
}
